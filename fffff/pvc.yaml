apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csv-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: openebs-jiva-csi-retain
  resources:
    requests:
      storage: 100Mi
