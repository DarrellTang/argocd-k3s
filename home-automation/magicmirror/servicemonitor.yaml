apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: magicmirror
  namespace: home-automation
  labels:
    app: magicmirror
spec:
  selector:
    matchLabels:
      app: magicmirror
  endpoints:
  - port: http
